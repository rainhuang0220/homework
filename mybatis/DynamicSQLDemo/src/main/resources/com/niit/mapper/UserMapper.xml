<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.niit.mapper.UserMapper">
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO Users(name, gender, age)
        values (#{name}, #{gender}, #{age})
    </insert>
    <select id="getAllUsers" parameterType="com.niit.model.User">
        SELECT * FROM Users
        <where>
        <if test="name != null">
            AND name=#{name}
        </if>
        <if test="gender != null">
            AND gender=#{gender}
        </if>
        <if test="age != null">
            AND age=#{age}
        </if>
        </where>
    </select>
    <update id="updateUser" parameterType="com.niit.model.User">
        UPDATE Users
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="gender != null and gender != ''">
                gender = #{gender},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
        </set>
        WHERE id = #{id}
    </update>
    <delete id="deleteUser" parameterType="int">
        DELETE FROM Users WHERE id = #{id}
    </delete>
</mapper>