# Spring Setter Injection Demo

A simple Spring application demonstrating Setter-Based Dependency Injection using XML configuration. The application injects a primitive value (name) and an object reference (Address) into a Student bean via setters, without using `new` keyword inside the Student class.
